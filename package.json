{
  "name": "binance-advisor",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "npm -w apps/api run dev",
    "dev:web": "npm -w apps/web run dev",
    "build": "npm -ws run build --if-present",
    "typecheck": "npm -ws run typecheck --if-present"
  },
  "engines": {
    "node": ">=18.18.0"
  }
}
