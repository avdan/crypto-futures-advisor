# This repo uses per-app env files.
#
# Server (secrets live here):
#   cp apps/api/.env.example apps/api/.env
#
# Web (public config, only VITE_* is exposed to the browser):
#   cp apps/web/.env.example apps/web/.env

